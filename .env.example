# Leave empty for local dev: Vite proxies /api → http://127.0.0.1:3001 (run the API separately).
# Production build: set to your API origin, no trailing slash, e.g. https://mbuzzi-api.onrender.com
VITE_API_BASE_URL=
